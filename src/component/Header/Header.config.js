import {
    Page as SourcePage,
} from 'SourceComponent/Header/Header.config';

// ? TODO: Since override is to JavaScript, you need to handle overridden ENUMS
export const Page = { ...SourcePage };
